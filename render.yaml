# Render Blueprint — Node serves game + login API + global leaderboard
services:
  - type: web
    name: space-invaders
    runtime: node
    buildCommand: npm install && npm run build:icons
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
